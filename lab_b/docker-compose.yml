version: '3.8'

services:
  nginx-https:
    build: .
    container_name: lab-b-nginx-https
    ports:
      - "80:80"    # HTTP port (will redirect to HTTPS)
      - "443:443"  # HTTPS port
    restart: unless-stopped
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
