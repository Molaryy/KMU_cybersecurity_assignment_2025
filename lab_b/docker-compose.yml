version: '3.8'

services:
  nginx-https:
    build: .
    container_name: lab-b-nginx-https
    ports:
      - "80:80"
      - "443:443"
    restart: unless-stopped
    networks:
      - lab-network

networks:
  lab-network:
    driver: bridge
